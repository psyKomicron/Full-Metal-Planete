using Metier;
using Metier.Carte.Cases;
using Metier.Unites;
using System;
using System.Collections.Generic;
using System.Text;

namespace Métier.Unites
{
    public class Reserve
    {
        //ici tu met ( (  (un/des) int) ou du genre) qui représente le nombre d'unité restantes 
        int Char = 16; // Exemple 

        /*
         * Le constructeur ici je sais pas si il faudrat des paramètre donc j'en met pas pour le moment je te laisse voir
         * (supprime les commentaire (sauf celle des 2 méthodes) quand t'as fini, même celle de ce constructeur)
         */
        public Reserve()
        {

        }

        /*
         * Cette méthode sert a la création de l'entité
         */
        public void Unit(TypeEntité t,Joueur j,Case c)
        {
            //tu utilise 
        }

        /*
         * Cette méthode permet de vérifié si il reste des entités 
         */
        private bool EstCreable(TypeEntité t)
        {
            bool temps = false;
                       //J'ai juste remplis parce qu'il me saoulé a pleuré qu'il avais pas de valeur de retour
            return temps;
        }

    }
}
